apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-data
  labels:
    app: market-data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: market-data
  template:
    metadata:
      labels:
        app: market-data
    spec:
      containers:
        - name: market-data
          image: ghcr.io/example/exf-market-data:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ENV
              valueFrom:
                configMapKeyRef:
                  name: exf-config
                  key: ENV
            - name: KAFKA_BROKERS
              value: kafka:9092
            - name: TIMESERIES_DSN
              value: postgres://exf:exf@postgres:5432/exf_ts?sslmode=disable
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: market-data
spec:
  selector:
    app: market-data
  ports:
    - name: http
      port: 80
      targetPort: 8080

